This folder preserves the original root-level static prototype that used direct `.html` links and standalone assets.

The runtime public site now lives in Django under `/backend`, using shared templates, named routes, and Django-managed static files.
